# Secrets for local GitHub Actions testing
# Copy this file to .secrets and fill in the actual values
# IMPORTANT: Never commit the actual .secrets file - keep it local only!

# AWS Credentials (required for Packer AMI build operations)
AWS_ACCESS_KEY_ID=your-aws-access-key-id
AWS_SECRET_ACCESS_KEY=your-aws-secret-access-key

# DevOps Repository Token (for triggering deployments)
DEVOPS_REPO_TOKEN=your-devops-repo-token

# Additional GitHub token (if needed)
GITHUB_TOKEN=your-github-token

# Notes:
# - AWS credentials should have EC2, AMI, and VPC permissions
# - DEVOPS_REPO_TOKEN needs repo dispatch permissions
# - For local testing, you may want to use non-production AWS accounts
